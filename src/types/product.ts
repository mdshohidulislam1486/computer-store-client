export type TProduct = {
  name: string;
  price: number;
  quantity: number;
  category: string;
  brand: string;
  compatibility: string;
  interface: string;
  condition: string;
  capacity: string;
  color: string;
  image: string;
  [key: string]: string | number;
};
